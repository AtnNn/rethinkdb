IssueComment
  { issueCommentUpdatedAt = 2014 (-09) (-03) 22 : 26 : 01 UTC
  , issueCommentUser =
      SimpleUser
        { simpleUserId = Id 316661
        , simpleUserLogin = N "timmaxw"
        , simpleUserAvatarUrl =
            "https://avatars.githubusercontent.com/u/316661?v=3"
        , simpleUserUrl = "https://api.github.com/users/timmaxw"
        , simpleUserType = OwnerUser
        }
  , issueCommentUrl =
      "https://api.github.com/repos/rethinkdb/rethinkdb/issues/comments/54377160"
  , issueCommentHtmlUrl =
      "https://github.com/rethinkdb/rethinkdb/issues/2924#issuecomment-54377160"
  , issueCommentCreatedAt = 2014 (-09) (-03) 22 : 26 : 01 UTC
  , issueCommentBody =
      "Also, under the current implementation, `r.db(\"rethinkdb\").table(\"cluster_config\").get(\"auth\").update(r.row)` is actually a no-op (it leaves the key unchanged) because the system sees `\"<hidden>\" == \"<hidden>\"` and so it never calls the function to write the \"new\" value of the row. This also means that `r.db(\"rethinkdb\").table(\"cluster_config\").get(\"auth\").update({\"auth_key\":\"<hidden>\"})` will return `{\"unchanged\":1}` if an auth key is set, although it will still produce the above error if no auth key is set."
  , issueCommentId = 54377160
  }