IssueComment
  { issueCommentUpdatedAt = 2013 (-05) (-23) 23 : 03 : 37 UTC
  , issueCommentUser =
      SimpleUser
        { simpleUserId = Id 258437
        , simpleUserLogin = N "srh"
        , simpleUserAvatarUrl =
            "https://avatars.githubusercontent.com/u/258437?v=3"
        , simpleUserUrl = "https://api.github.com/users/srh"
        , simpleUserType = OwnerUser
        }
  , issueCommentUrl =
      "https://api.github.com/repos/rethinkdb/rethinkdb/issues/comments/18377356"
  , issueCommentHtmlUrl =
      "https://github.com/rethinkdb/rethinkdb/issues/867#issuecomment-18377356"
  , issueCommentCreatedAt = 2013 (-05) (-23) 23 : 03 : 37 UTC
  , issueCommentBody =
      "See the table at the bottom of http://www.regular-expressions.info/refflavors.html\r\n\r\nThe following program prints `No match found.` on OS X and `Match found!` on Linux.\r\n\r\n```c\r\n#include <regex.h>\r\n#include <stdio.h>\r\n#include <stdlib.h>\r\n\r\nint main(void) {\r\n  regex_t r;\r\n  int res = regcomp(&r, \"\\\\w\", REG_EXTENDED);\r\n  if (res != 0) {\r\n    perror(\"regcomp fail\");\r\n    exit(EXIT_FAILURE);\r\n  }\r\n\r\n  const int nmatch = 1;\r\n  regmatch_t pmatch[nmatch];\r\n  res = regexec(&r, \"a\", nmatch, pmatch, 0);\r\n  if (res == 0) {\r\n    printf(\"Match found!\\n\");\r\n  } else if (res == REG_NOMATCH) {\r\n    printf(\"No match found.\\n\");\r\n  } else {\r\n    printf(\"Some other return value.\\n\");\r\n  }\r\n\r\n  return 0;\r\n}\r\n```\r\n\r\nAlso, the man pages of regexec and the like don't mention unicode on either OS X or Linux.  I think you have to get the locale settings right for that to work properly (on Linux), I have no idea about OS X."
  , issueCommentId = 18377356
  }