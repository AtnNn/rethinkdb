IssueComment
  { issueCommentUpdatedAt = 2013 (-10) (-04) 17 : 09 : 10 UTC
  , issueCommentUser =
      SimpleUser
        { simpleUserId = Id 706854
        , simpleUserLogin = N "AtnNn"
        , simpleUserAvatarUrl =
            "https://avatars.githubusercontent.com/u/706854?v=3"
        , simpleUserUrl = "https://api.github.com/users/AtnNn"
        , simpleUserType = OwnerUser
        }
  , issueCommentUrl =
      "https://api.github.com/repos/rethinkdb/rethinkdb/issues/comments/25714719"
  , issueCommentHtmlUrl =
      "https://github.com/rethinkdb/rethinkdb/issues/1511#issuecomment-25714719"
  , issueCommentCreatedAt = 2013 (-10) (-04) 17 : 09 : 10 UTC
  , issueCommentBody =
      "Note that @jdoliner's proposal does not affect the first query, where `r.any` returns `{a:2}` which is used by `filter`.\r\n\r\nI think adding more implicit defaults is not a good idea, but if we add it to `r.any` should be added to `r.not` and other operators because in queries like `r.any(..., r.not(foo))`, the user might expect `r.not(foo.default(False))` instead of `r.not(foo).default(False)`"
  , issueCommentId = 25714719
  }