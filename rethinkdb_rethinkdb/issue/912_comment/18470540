IssueComment
  { issueCommentUpdatedAt = 2013 (-05) (-26) 22 : 07 : 54 UTC
  , issueCommentUser =
      SimpleUser
        { simpleUserId = Id 505365
        , simpleUserLogin = N "danielmewes"
        , simpleUserAvatarUrl =
            "https://avatars.githubusercontent.com/u/505365?v=3"
        , simpleUserUrl = "https://api.github.com/users/danielmewes"
        , simpleUserType = OwnerUser
        }
  , issueCommentUrl =
      "https://api.github.com/repos/rethinkdb/rethinkdb/issues/comments/18470540"
  , issueCommentHtmlUrl =
      "https://github.com/rethinkdb/rethinkdb/issues/912#issuecomment-18470540"
  , issueCommentCreatedAt = 2013 (-05) (-26) 22 : 07 : 40 UTC
  , issueCommentBody =
      "It seems that it is in general not possible to generate an array of sequences.\r\nIf I do ```r.expr([r.table('t1'), r.table('t1')])``` I get the same error (\"Expected type DATUM but found TABLE\").\r\n\r\nWith get instead of getAll it works because get returns a datum, while getAll returns a sequence.\r\n\r\nThere is a workaround though: You can use coerce_to('array') to convert the sequence into a datum:\r\n``` r.expr(a).map(lambda x: r.table('applications').get_all(x, index='agent_id').coerce_to('array')).run(conn) ```\r\n\r\nIt might also be convenient to use concat_map instead of map here, but I'm not sure if that is necessary."
  , issueCommentId = 18470540
  }