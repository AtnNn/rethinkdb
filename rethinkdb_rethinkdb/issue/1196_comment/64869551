IssueComment
  { issueCommentUpdatedAt = 2014 (-11) (-28) 08 : 51 : 11 UTC
  , issueCommentUser =
      SimpleUser
        { simpleUserId = Id 1461947
        , simpleUserLogin = N "neumino"
        , simpleUserAvatarUrl =
            "https://avatars.githubusercontent.com/u/1461947?v=3"
        , simpleUserUrl = "https://api.github.com/users/neumino"
        , simpleUserType = OwnerUser
        }
  , issueCommentUrl =
      "https://api.github.com/repos/rethinkdb/rethinkdb/issues/comments/64869551"
  , issueCommentHtmlUrl =
      "https://github.com/rethinkdb/rethinkdb/issues/1196#issuecomment-64869551"
  , issueCommentCreatedAt = 2014 (-11) (-28) 08 : 51 : 11 UTC
  , issueCommentBody =
      "Quick note about the nestedness of `r.row`. We currently forbid nested `r.row`, but there's one little exception to this rule, we can write:\r\n\r\n```\r\nr.table(\"foo\").orderBy(r.desc(r.row(\"bar\")).run(...)\r\n```\r\n\r\nAnd `orderBy` and `r.desc` can both take a function.\r\nIt's kind of an edge case but it may be worth mentioning for developers as this is not a valid syntax:\r\n```\r\nr.table(\"foo\").orderBy(function(doc) { return r.desc(doc(\"bar\")) })\r\n```\r\n\r\nBut this one is:\r\n```\r\nr.table(\"foo\").orderBy(r.desc(function(doc) { return doc(\"bar\") }))\r\n```"
  , issueCommentId = 64869551
  }